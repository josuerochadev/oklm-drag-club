# URL publique du site (sans slash final)
# En production Vercel, VERCEL_URL est injecté automatiquement comme fallback.
NEXT_PUBLIC_SITE_URL=https://oklmdragclub.com

# Webhook de déploiement Vercel (utilisé par /api/rebuild)
VERCEL_DEPLOY_HOOK_URL=https://api.vercel.com/v1/integrations/deploy/...

# Secret partagé pour protéger /api/rebuild
# OBLIGATOIRE : remplacer par une valeur forte avant de déployer.
# Générer avec : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
REBUILD_SECRET=REPLACE_WITH_STRONG_SECRET

# Sentry — monitoring des erreurs (https://sentry.io)
# DSN disponible dans : Settings → Projects → <projet> → Client Keys
NEXT_PUBLIC_SENTRY_DSN=

# Sentry — upload des source maps (CI uniquement, à stocker dans GitHub Secrets)
# Token disponible dans : Settings → Auth Tokens (scope : project:releases + org:read)
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
